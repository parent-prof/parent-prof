{% extends 'base.html.twig' %}

{% block title %}Edit Promotion{% endblock %}

{% block body %}
    <h1>Edit Promotion</h1>

    {{ include('prof/promotion/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('prof_promotion_index') }}">back to list</a>

    {{ include('prof/promotion/_delete_form.html.twig') }}
{% endblock %}
