{% extends 'base.html.twig' %}

{% block title %}Professeur{% endblock %}

{% block body %}
    <h1>Professeur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ professeur.id }}</td>
            </tr>
            <tr>
                <th>Id_Professeur</th>
                <td>{{ professeur.idProfesseur }}</td>
            </tr>
            <tr>
                <th>Id_connexion</th>
                <td>{{ professeur.idConnexion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('professeur_index') }}">back to list</a>

    <a href="{{ path('professeur_edit', {'id': professeur.id}) }}">edit</a>

    {{ include('professeur/_delete_form.html.twig') }}
{% endblock %}
