{% extends 'base.html.twig' %}

{% block title %}Eleve{% endblock %}

{% block body %}
    <h1>Eleve</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Profil</th>
                <td><img src="{{ asset('uploads/images/products/') }}{{ eleve.image }}" width="20" class="small-img" height="150"></td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ eleve.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ eleve.matricule }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ eleve.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ eleve.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('parent_eleve_index') }}">back to list</a>

    <a href="{{ path('parent_eleve_edit', {'id': eleve.id}) }}">edit</a>

    {{ include('parent/eleve/_delete_form.html.twig') }}
{% endblock %}
