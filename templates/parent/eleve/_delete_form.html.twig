<form method="post" action="{{ path('parent_eleve_delete', {'id': eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eleve.id) }}">
    <button class="btn">Delete</button>
</form>
